import { PlaceholderSection } from '@/components/dashboard/placeholder-section';

export default function AreasPage() {
  return (
    <PlaceholderSection
      title="Áreas"
      description="Esta ruta mantiene la IA del dashboard y comunica de forma explícita que Áreas todavía no está activa en esta versión."
      tone="muted"
    />
  );
}

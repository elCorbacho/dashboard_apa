import { PlaceholderSection } from '@/components/dashboard/placeholder-section';

export default function TecnicasPage() {
  return (
    <PlaceholderSection
      title="Técnicas"
      description="La ruta Técnicas se conserva visible para mantener la estructura de navegación, pero su implementación funcional queda fuera del alcance de este batch."
      tone="muted"
    />
  );
}

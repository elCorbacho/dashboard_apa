import { PlaceholderSection } from '@/components/dashboard/placeholder-section';

export default function WidgetsPage() {
  return (
    <PlaceholderSection
      title="Widgets"
      description="Esta ruta preserva la arquitectura de navegación, pero sigue en estado placeholder en v1. La funcionalidad real se implementará una vez cerrada la paridad del Overview."
      tone="muted"
    />
  );
}
